[project]
name = "ptcg-obb-training"
version = "0.1.0"
description = "Synthetic dataset generation and YOLO-OBB training for Pokémon TCG card detection"
requires-python = ">=3.10,<3.13"
dependencies = [
    "ultralytics>=8.3.0",
    "opencv-python>=4.10.0",
    "numpy>=1.26.0",
    "tqdm>=4.66.0",
]

[project.optional-dependencies]
# TFJS export pulls heavy deps (tensorflow + tensorflowjs converter). Install on demand:
#   uv sync --extra export
export = [
    "tensorflow>=2.16.0,<2.20.0",
    "tensorflowjs>=4.20.0",
]

# For CUDA torch, add to pyproject.toml *before* `uv sync`:
#
#   [[tool.uv.index]]
#   name = "pytorch-cu121"
#   url = "https://download.pytorch.org/whl/cu121"
#   explicit = true
#
#   [tool.uv.sources]
#   torch = { index = "pytorch-cu121" }
#   torchvision = { index = "pytorch-cu121" }
